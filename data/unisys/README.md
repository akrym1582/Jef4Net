# Unisys LETS-J generation data

These deterministic two-column snapshots contain the Unicode JIS records consumed
by `UnisysMappingGenerator`. CodeGen derives LETS-J bytes; runtime code does not
load these files. See `provenance.json` and the third-party notices.
